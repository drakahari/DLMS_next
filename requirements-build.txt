# Canonical DLMS 3.2.2 build tools. Install this after the locked runtime
# environment; it intentionally does not replace requirements-lock.txt.
-r requirements-lock.txt
PyInstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
