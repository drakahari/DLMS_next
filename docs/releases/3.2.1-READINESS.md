# DLMS 3.2.1 source release-readiness review

Date: September 23, 2026. Repository: `drakahari/DLMS_next`.
Branch: `develop/3.2.1`. Starting HEAD:
`1e8fe20dce0cfda9fa575009d33fd56293711324`.
Assessed state: that commit plus this uncommitted source/version/documentation
diff. The initial working tree was clean. No final release commit exists from
this task; existing ignored binaries are historical and were not reused.

## Source-freeze conclusion

Review disposition: **READY FOR OWNER REVIEW — SOURCE READY TO FREEZE**.
Both complete source gates passed. No implementation defect requiring a
pre-freeze fix was identified.
Owner review and committing the intended diff to produce a clean frozen checkout
remain required before invoking native release builds. An uncommitted review
tree is not itself a frozen build input: the native orchestrator correctly
rejects it. Native artifacts and their acceptance occur after freeze and are
not failed source gates.

## Requirement classification

| Requirement | Classification | Evidence / next action |
| --- | --- | --- |
| 1. Source-tree cleanliness | READY FOR OWNER REVIEW | Started clean; only intended text/test/audit edits. Owner reviews and commits them; clean frozen checkout is required for builds. |
| 2. APP_VERSION = 3.2.1 | COMPLETE | Runtime constant, UI/Help, export/backup assertions, spec-derived macOS metadata, source build/install guidance and support assets converge. |
| 3. GitHub roadmap/open issues | COMPLETE | Live Project #1 and all open issues reconciled; only #4 / DLMS-145 remains, classified below. |
| 4. Full non-browser gate | COMPLETE | 1,794 passed; 104 browser tests deselected; 2,450 subtests passed. |
| 5. Full Firefox gate | COMPLETE | Enabled Firefox gate: 96 passed, none skipped. |
| 6. Documentation/manual/tutorial status | READY FOR OWNER REVIEW | Current text and definitions validated; historical screenshots retained. Eight tutorial candidates need separate owner listening/editorial review, optional for application release. |
| 7. Windows icon acceptance | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | Native rebuild, Explorer and fresh normal shortcut, expected icon/no fallback, cache observations and human acceptance. |
| 8. macOS icon acceptance | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | Native Finder/Dock checks and no branding regression, with exact artifact/source and human acceptance recorded. |
| 9. AppImage final-artifact cross-distribution acceptance | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | For the final optional image, same frozen Ubuntu-baseline bytes on Fedora, both Ubuntu targets and Omarchy; retain per-host evidence and image hash. DLMS-144 is complete. |
| 10. Six native target builds | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | Fedora 44, Ubuntu 24.04, Ubuntu 26.04, Omarchy Quattro, Windows 11, macOS arm64; exact same final source commit. |
| 11. Frozen release commit | REQUIRED BEFORE RELEASE FREEZE | Owner review/commit and normal branch finalization; record full SHA and clean tree. No commit performed here. |
| 12. Downloaded-release acceptance | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | After upload availability, verify actual bytes against tag-bound contract and native build hashes; native browser-download security prompts need real hosts. |
| 13. Checksums | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | Hash final archives on native hosts, verify unchanged transfer, assemble canonical six-file manifest; separate AppImage record. |
| 14. GitHub release publication | REQUIRED DURING NATIVE BUILD/ACCEPTANCE | Owner-controlled tagging/upload/publication after artifact gates; no publication performed here. |
| Manual PDF, tutorial candidate approval/publication, broader assistive-technology review | OPTIONAL / POST-RELEASE | Useful follow-up, not manufactured roadmap scope or an application-freeze blocker. |

The exact downstream sequence is in the [3.2.1 execution checklist](3.2.1-RELEASE-CHECKLIST.md).
If native acceptance reveals a real source defect, fix and validate it, establish
a new freeze and rebuild consistently; never rename older binaries as 3.2.1.

## Roadmap reconciliation and DLMS-145

Live GitHub reads on September 23 returned one open repository issue and one
Project #1 item: [#4, DLMS-145 — Native Icon Visual Acceptance](https://github.com/drakahari/DLMS_next/issues/4).
Fields: Planned; Runtime & Packaging; 3.2.1; Hardening; P2 — Normal.
There were no issue comments or additional recorded dependencies. The issue
itself records DLMS-144 implementation, the Omarchy browser fix and previous
cross-distribution validation complete. No historical work was reopened and no
new issue or DLMS ID was created. GitHub's current published stable tag is
v3.2.0 (September 20 UTC), separate from this source target.

This Fedora 44 x86_64 host can inspect and test portable icon generation/spec
wiring. `tools/native_icons.py` derives the genuine Windows ICO from
`static/favicon.ico`; `DLMS.spec` supplies that generated path on Windows and
preserves the existing source on macOS. The portable test executes each platform
branch with mocked EXE/BUNDLE calls, verifies ICO sizes 16/24/32/48/64/128/256,
and checks macOS artwork selection. AppDir artwork pixels/desktop layout are
also tested. These checks do not build an EXE/app or prove native shell rendering.
No icon implementation change was necessary.

The current-host preparation is complete. Native Windows/macOS execution is not
available in this task and final packages are explicitly out of scope. The exact
manual checks are listed in the execution checklist. Since native implementation
or acceptance has not begun, **Project status remains Planned**; moving this
acceptance-only item to Ready for Review would falsely imply required validation
was complete. No Project status updates, issue closure or Done transition were
made. The [host-preparation report](https://github.com/drakahari/DLMS_next/issues/4#issuecomment-5794371573)
was added to issue #4 after final validation; native acceptance remains explicitly
outstanding. A subsequent GitHub read confirmed OPEN / Planned.

Repository-local review also found real editorial acceptance work:
`docs/demo-video/series/README.md` and `editorial-status.json` record six approved
phase-one tutorials and seven phase-two candidates awaiting owner listening and
editorial review. Parsing tutorial 14 is another explicit candidate. Their
definitions/manual references/capture hashes can be validated now; listening,
editorial approval and optional publication remain owner work. Older overview
candidate notes are historical: the current README records its published URL.
No historical audit suggestion is treated as a new required issue. Storage
DLMS-146/147/149 records describe implemented safeguards and retained resource
limits; this task does not reopen them or allocate follow-up IDs.

## Version convergence and preservation

The [complete version inventory](../audits/2026-09-23-version-convergence.md)
classifies every remaining old-version text location as historical/intentional
or isolated test data. No stale active 3.2.0 application identifier remains.
`DLMS.spec` already derives version fields; no redundant constant was added.

Old release notes, audits, screenshot plans/manifests, capture metadata and
generated artifacts were retained. No manual/tutorial screenshot was retaken,
regenerated or replaced for a version-only difference. The capture regression
now compares historical metadata to its original manifest and independently
checks current-version discovery for future captures. No data schema, scoring,
parser or native-icon behavior was changed by this convergence task.

## Validation evidence

Project environment: `.venv`, Python 3.14.7, Fedora 44 x86_64.
Commands use the activated environment and `PYTHONDONTWRITEBYTECODE=1`.

- Focused release/version/package/OCR/Help/manual/tutorial/backup/export/repository
  tests: **292 passed, 717 subtests passed in 7.93s**. The initial run found the
  obsolete capture-version equality assertion (290 passed, one failed); it was
  corrected for the explicit screenshot-preservation requirement, not bypassed.
- `python -m pytest -q -p no:cacheprovider -m "not browser"`:
  **1,794 passed, 104 deselected, 2,450 subtests passed in 25.50s**.
- With `DLMS_RUN_BROWSER_TESTS=1`,
  `python -m pytest -q -p no:cacheprovider -m browser tests/browser/test_critical_workflows.py`:
  **96 passed in 396.95s**, none skipped. The sandbox attempt failed before browser
  setup because socket creation was prohibited (93 setup errors, three failures).
  The successful full gate ran outside that socket restriction; no skip is counted
  as a pass. Fixtures terminate their own Firefox/server process groups in finally
  blocks. The post-run process check found no task-owned browser/server left
  running. An unrelated Firefox process started September 21 was left untouched.
- `python tools/build_documentation_videos.py validate --all`: **14 valid
  definitions**, with **93 capture references**, source hashes and manual anchors.
- `python tools/build_demo_video.py validate`: **43 screenshots validated**;
  main=40, long=41; 40 main scenes / 451 seconds. Audio was not required by this
  definition validator; this does not claim a new listening or media-render audit.
- `python -m pip check`: **No broken requirements found**. No fresh vulnerability
  advisory scan was run; scheduled dependency auditing remains separate.
- Local Markdown validation: **363 links across 42 documents passed**, including
  current release/audit documents and all manual chapters; two intentional
  GitHub-relative Releases links were excluded from filesystem checks.
- Remaining-version inventory: **no stale active identifiers**; each historical,
  published-version and isolated synthetic-fixture occurrence is classified.
- `git diff --check`: **passed**. Generated/screenshot/artifact and historical
  release/audit diff checks: **no changes**. No generated binary was added.

Only metadata/source contract tests used synthetic package headers/archives in
temporary test directories. No real native release package, AppImage, screenshot,
audio or video was built as part of this review.

## Quality assessments

The [September 5 rubric reassessment](../audits/2026-09-23-historical-product-quality-assessment.md)
scores **9.792/10** (9.79 rounded), versus 9.747: **+0.045**. Product coherence,
backup compatibility/capacity protection, UX and documentation account for the
changes. All twelve categories, weights, contributions and reasons are recorded.

The [maturity rubric reassessment](../audits/2026-09-23-mature-product-assessment.md)
scores **9.510/10** (9.51), versus 9.45: **+0.060**. Product coherence, usability
and documentation account for the changes. Maintainability, manual accessibility
limits and exact-native acceptance boundaries remain. Neither score awards
planned work or native acceptance absent evidence; neither is a publication gate.

## Files and ownership

Version-bearing files are individually listed in the version inventory. Additional
changes are `tests/test_manual_screenshot_capture.py`, the audit index, three new
September 23 audit records and three new `docs/releases/3.2.1*` documents.
No unrelated user changes existed or were overwritten. The diff contains text,
metadata assertions and the capture-provenance test correction only.

No commit, push, merge, tag, release upload or publication was performed. No final
native package was built, and no old artifact was promoted. Owner review and a
clean frozen commit remain separate from this local preparation.
